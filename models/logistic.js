class Logistic {
  constructor(product_id, stock, supplier_id, supplier_price) {
    this.product_id = product_id;
    this.stock = stock;
    this.supplier_id = supplier_id;
    this.supplier_price = supplier_price;
  }
}

module.exports = Logistic;
